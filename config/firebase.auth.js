import admin from "firebase-admin";

// This file no longer initializes Firebase.
// It simply imports the admin object so other files can use
// the instance that was initialized in server.js.
// You can remove the "Firebase connected" log from here.

export default admin;